{% extends 'base.html.twig' %}

{% block title %}Shop{% endblock %}

{% block body %}
    <h1>Shop</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shop.id }}</td>
            </tr>
            <tr>
                <th>Uuid</th>
                <td>{{ shop.uuid }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ shop.Name }}</td>
            </tr>
            <tr>
                <th>ExternalId</th>
                <td>{{ shop.ExternalId }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ shop.Image }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ shop.Url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('shop_index') }}">back to list</a>

    <a href="{{ path('shop_edit', {'id': shop.id}) }}">edit</a>

    {{ include('shop/_delete_form.html.twig') }}
{% endblock %}
