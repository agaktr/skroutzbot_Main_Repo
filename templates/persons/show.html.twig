{% extends 'base.html.twig' %}

{% block title %}Persons{% endblock %}

{% block body %}
    <h1>Persons</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ person.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ person.Name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ person.Surname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('persons_index') }}">back to list</a>

    <a href="{{ path('persons_edit', {'id': person.id}) }}">edit</a>

    {{ include('persons/_delete_form.html.twig') }}
{% endblock %}
